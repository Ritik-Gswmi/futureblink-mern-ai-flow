# If set, frontend will call the deployed backend directly (recommended for production).
# Leave empty for local dev (Vite proxy will forward /api/* to http://localhost:5000).
VITE_API_URL=https://mern-ai-flow-backend.onrender.com

